{
  "$schema-comment": "Configuration for the codex-workflow plugin when working inside codex-workflows-plugin itself. Extends the default allowlist to cover this repo's own documentation and release files.",
  "codex": {
    "folder": "AI_Codex"
  },
  "markdownAllowlist": {
    "patterns": [
      "CLAUDE.md",
      "GEMINI.md",
      ".agent/**",
      "AI_Codex/**",
      "CHANGELOG.md",
      "README.md",
      "docs/**",
      "skills/**/*.md",
      "commands/**",
      ".claude-plugin/**",
      ".codex-plugin/**"
    ]
  }
}
